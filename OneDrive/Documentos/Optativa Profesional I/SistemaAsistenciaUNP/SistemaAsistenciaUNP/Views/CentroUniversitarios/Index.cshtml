@model IEnumerable<SistemaAsistenciaUNP.Models.CentroUniversitario>

@{
    ViewData["Title"] = "Centros Universitarios";
}

<div class="container mt-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="fw-bold text-dark">Centros Universitarios</h1>
            <p class="text-muted">
                Gestión de sedes y CUR de la Universidad Nacional Politécnica.
            </p>
        </div>

        <div>
            <a asp-action="Create" class="btn btn-primary shadow-sm">
                Nuevo Centro
            </a>
        </div>
    </div>

    <div class="card shadow-sm border-0 rounded-4">
        <div class="card-body">

            <table class="table table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Nombre</th>
                        <th>Dirección</th>
                        <th>Teléfono</th>
                        <th>Estado</th>
                        <th width="220"></th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="fw-semibold">
                                @item.Nombre
                            </td>

                            <td>
                                @item.Direccion
                            </td>

                            <td>
                                @item.Telefono
                            </td>

                            <td>
                                @if (item.Estado)
                                {
                                    <span class="badge bg-success">
                                        Activo
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">
                                        Inactivo
                                    </span>
                                }
                            </td>

                            <td>
                                <a asp-action="Edit"
                                   asp-route-id="@item.CentroUniversitarioId"
                                   class="btn btn-sm btn-outline-primary">
                                    Editar
                                </a>

                                <a asp-action="Details"
                                   asp-route-id="@item.CentroUniversitarioId"
                                   class="btn btn-sm btn-outline-secondary">
                                    Detalles
                                </a>

                                <a asp-action="Delete"
                                   asp-route-id="@item.CentroUniversitarioId"
                                   class="btn btn-sm btn-outline-danger">
                                    Eliminar
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>
    </div>

</div>